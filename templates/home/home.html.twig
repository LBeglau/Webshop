{% extends 'base.html.twig' %}
{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/home.css') }}">
    {{ parent() }}
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.10.2/dist/umd/popper.min.js" integrity="sha384-7+zCNj/IqJ95wo16oMtfsKbZ9ccEh31eOz1HGyDuCQ6wgnyJNSYdrPa03rtR1zdB" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.min.js" integrity="sha384-QJHtvGhmr9XOIpI6YVutG+2QOK9T+ZnN4kzFN1RtK3zEFEIsxhlmWl5/YESvpZ13" crossorigin="anonymous"></script>
{% endblock %}
{% block title %}Products{% endblock %}
{% block body %}
<div class="container">
    <div class="body-Cards">
        {# {{ dump(user) }} #}
        {# {{ dump(products) }} #}
        {% for product in products %}
            {% if product.id == 1 %}
            <div class="seperate-Cards">
                <div class="card">
                    <div class="imgBox">
                        <img src="{{ asset('resources/pc.png') }}">
                    </div>
                    <div class="contentBox">
                        <h2>{{ product.name }}</h2>
                        <h3>{{ product.price }}€</h3>
                        {% if user is defined %}
                            <input type="button" id="1" class="buy" value="Kaufen" href="{{ path('/login') }}">
                        {% else %}
                            <input type="button" id="1" class="buy" value="Kaufen" onclick="buyProduct(1)">
                        {% endif %}
                        {{ product.description }}
                    </div>
                </div>
            </div>
            {% elseif product.id == 2  %}
                <div class="seperate-Cards">
                    <div class="card">
                        <div class="imgBox">
                            <img src="{{ asset('resources/drucker.png') }}">
                        </div>
                        <div class="contentBox">
                            <h2>{{ product.name }}</h2>
                            <h3>{{ product.price }}€</h3>
                            {% if user is defined %}
                                <input type="button" id="1" class="buy" value="Kaufen" href="{{ path('/login') }}">
                            {% else %}
                                <input type="button" id="1" class="buy" value="Kaufen" onclick="buyProduct(2)">
                            {% endif %}
                            {{ product.description }}
                        </div>
                    </div>
                </div>
            {% elseif product.id == 3  %}
                <div class="seperate-Cards">
                    <div class="card">
                        <div class="imgBox">
                            <img src="{{ asset('resources/lautsprecher.png') }}">
                        </div>
                        <div class="contentBox">
                            <h2>{{ product.name }}</h2>
                            <h3>{{ product.price }}€</h3>
                            {% if user is defined %}
                                <input type="button" id="1" class="buy" value="Kaufen" href="{{ path('/login') }}">
                            {% else %}
                                <input type="button" id="1" class="buy" value="Kaufen" onclick="buyProduct(3)">
                            {% endif %}
                            {{ product.description }}
                        </div>
                    </div>
                </div>
            {% elseif product.id == 4  %}
                <div class="seperate-Cards">
                    <div class="card">
                        <div class="imgBox">
                            <img src="{{ asset('resources/scanner.png') }}">
                        </div>
                        <div class="contentBox">
                            <h2>{{ product.name }}</h2>
                            <h3>{{ product.price }}€</h3>
                            {% if user is defined %}
                                <input type="button" id="1" class="buy" value="Kaufen" href="{{ path('/login') }}">
                            {% else %}
                                <input type="button" id="1" class="buy" value="Kaufen" onclick="buyProduct(4)">
                            {% endif %}
                            {{ product.description }}
                        </div>
                    </div>
                </div>
            {% elseif product.id == 5  %}
                <div class="seperate-Cards">
                    <div class="card">
                        <div class="imgBox">
                            <img src="{{ asset('resources/cd-laufwerk.png') }}">
                        </div>
                        <div class="contentBox">
                            <h2>{{ product.name }}</h2>
                            <h3>{{ product.price }}€</h3>
                            {% if user is defined %}
                                <input type="button" id="1" class="buy" value="Kaufen" href="{{ path('/login') }}">
                            {% else %}
                                <input type="button" id="1" class="buy" value="Kaufen" onclick="buyProduct(5)">
                            {% endif %}
                            {{ product.description }}
                        </div>
                    </div>
                </div>
            {% endif %}
        {% endfor %}
    </div>
    <script>
        function buyProduct(productId) {
            /** Ersetzen der URL '/buy/pId', da Twig vor JS gerendert wird und demnach die URL noch nicht erkannt wird **/
            var url = '{{ path('buy', {'pId': 'productId'}) }}';
            url = url.replace('productId', productId);

            $.ajax( url, {
                type: 'GET',
                data: productId,
                success: function () {
                        //document.location.reload();
                },
                error: function () {
                }
            });
        }
    </script>
</div>
{% endblock %}
{% block javascripts %}
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
{% endblock %}


